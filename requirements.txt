# 项目依赖文件
Flask==2.0.1
Werkzeug==2.0.3
APScheduler==3.9.1
requests==2.28.1
beautifulsoup4==4.11.1
feedgen==0.9.0
selenium==4.8.0
lxml==4.9.2
python-dateutil==2.8.2

# 通知系统依赖 - 可选安装
# 如需使用完整通知功能，请根据需要安装以下依赖：
# 
# 邮件通知 - 无需额外依赖
# Webhook通知 - 无需额外依赖
# 桌面通知:
#   pip install plyer==2.1.0  # 跨平台桌面通知
#   pip install win10toast==0.9  # Windows桌面通知 (仅Windows系统需要)
# Telegram通知 - 无需额外依赖
# 企业微信通知 - 无需额外依赖